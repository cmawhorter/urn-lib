export const kParsedProtocol = Symbol('parsed_protocol_value');

/**
 * @deprecated
 * The key previously used within a parsed urn to store
 * the protocol. This has since been moved into a symbol
 * key `kParsedProtocol`.
 */
export const kLegacyParsedProtocol = 'protocol';
